/**
 * Shared constants for whiteboard-related functionality.
 */

/**
 * Directory name for temporary whiteboard image storage.
 * Images are stored in: {globalStorageUri}/{TEMP_IMAGE_DIRECTORY}/
 */
export const TEMP_IMAGE_DIRECTORY = 'temp-whiteboard-images';
